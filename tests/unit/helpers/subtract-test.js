
import { subtract } from 'odototo/helpers/subtract';
import { module, test } from 'qunit';

module('Unit | Helper | subtract');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subtract([42]);
  assert.ok(result);
});

